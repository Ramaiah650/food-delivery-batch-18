{
  "name": "food-delivery-app",
  "version": "1.0.1",
  "private": true,
  "description": "FoodHub frontend + backend. Run npm commands from this folder (project root).",
  "scripts": {
    "seed": "npm run seed --prefix backend",
    "start": "npm start --prefix backend",
    "dev": "npm run dev --prefix backend",
    "frontend": "npx --yes serve . -l 3000",
    "free-port": "npx --yes kill-port 5000",
    "backend": "npm start --prefix backend"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.5.0"
  }
}
